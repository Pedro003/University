#include "Estrutura.h"

int main(int argc, char *argv[]) {
    if (argc != 4 && argc != 3) {
        perror("Número de argumentos incorreto");
        return 1;
    }

    struct Pessoa pessoas[BUFFER_SIZE]; // Alocando espaço para armazenar pessoas
    if (argv[1] == -i) {
        
    }
    else if (argv[1] == -l){

    }
    else if (argv[1] == -u){

    }
    else{
        perror("argumento %s não suportado", argv[1]);
    }

    return 0;
}












